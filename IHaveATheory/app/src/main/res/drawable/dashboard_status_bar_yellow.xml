<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background properties -->
    <item android:id="@android:id/background">
        <shape>
            <size
                android:minHeight="5dip"
                android:height="5dp"
                android:maxHeight="5dip"/>
            <corners
                android:radius="2dp"/>
            <gradient
                android:angle="270"
                android:centerColor="#1a1a1a"
                android:centerY="1.0"
                android:endColor="#0d1522"
                android:startColor="#000001" />
        </shape>
    </item>

    <!-- Progress properties -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <size
                    android:minHeight="5dip"
                    android:height="5dp"
                    android:maxHeight="5dip"/>
                <corners
                    android:radius="2dp"/>
                <gradient
                    android:angle="270"
                    android:centerColor="#f8c523"
                    android:centerY="1.0"
                    android:endColor="#f3ab10"
                    android:startColor="#fad440" />
            </shape>
        </clip>
    </item>

</layer-list>